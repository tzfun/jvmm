import javax.tools.ToolProvider

version '0.1'

dependencies {
    compile 'io.netty:netty-all:4.1.63.Final'
    compile(project(':core'))
    compileOnly(files(((URLClassLoader) ToolProvider.getSystemToolClassLoader()).getURLs()))
}

jar {
    manifestContentCharset 'utf-8'
    metadataCharset 'utf-8'

    archiveName("jvmm-client.jar")
    manifest{
        attributes 'Main-Class': 'org.beifengtz.jvmm.client.ClientBootstrap'
    }

    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}